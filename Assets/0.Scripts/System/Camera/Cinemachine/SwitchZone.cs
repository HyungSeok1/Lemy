using UnityEngine;


/// <summary>
/// 일정 존에 들어오면 바로 CameraSwitcher에게 신호를 보내 카메라를 전환시킵니다.
/// 
/// 잠깐 전부 주석처리해두었습니다.
/// 
/// </summary>
public class SwitchZone : MonoBehaviour
{


    //    void OnTriggerEnter2D(Collider2D other)
    //    {
    //        prevCam = CameraSwitcher.ActiveCamera;

    //        if (other.gameObject.CompareTag("Player") && CameraSwitcher.ActiveCamera != cam)
    //            CameraSwitcher.SwitchCamera(cam);
    //    }

    //    void OnTriggerExit2D(Collider2D other)
    //    {
    //        prevCam = CameraSwitcher.ActiveCamera;

    //        if (other.gameObject.CompareTag("Player") && CameraSwitcher.ActiveCamera == cam)
    //            CameraSwitcher.SwitchCamera(cam);
    //    }
    //}
}
